# Copy this file to `.env` and fill in real values
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-username@example.com
SMTP_PASS=your-smtp-password

# Where to send contact form messages (your email)
# Set this to your Gmail address to receive messages
MAIL_TO=jayakumar03052005@gmail.com
# Optional from address (if unset, SMTP_USER will be used)
MAIL_FROM=website@example.com

# Example settings for Gmail (use an App Password if your account has 2FA):
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=465
# SMTP_SECURE=true
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
